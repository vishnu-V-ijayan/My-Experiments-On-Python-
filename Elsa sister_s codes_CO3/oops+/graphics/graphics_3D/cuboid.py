def cubArea(length,breadth,height):

    area=2 * (length * breadth + breadth * height + length * height)
    return area

def cubPerimeter(length,breadth,height):

    perimeter=4*(length*breadth*height)
    return perimeter